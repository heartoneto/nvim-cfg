require'tokyonight'.setup({
    -- Style inside the theme
    style = "storm",
    -- Allow transparent
    transparent = true,
    -- Dim inactive windows
    dim_inactive = true,
    -- Use this theme on terminals
    terminal_colors = true,
})
